{
  "name": "@agentix-e/causality-analyzer-pipeline",
  "version": "0.1.0",
  "description": "Causality Analyzer Pipeline — anomaly detection, causal graph discovery, RCA analysis, causal inference",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agentix-e/causality-analyzer-core": "workspace:*",
    "simple-statistics": "^7.8.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@vitest/coverage-v8": "^3.0.0"
  },
  "license": "MIT"
}
